python-telegram-bot[job-queue]>=20.0
sympy>=1.12
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pint>=0.22
cryptography>=41.0
openai>=1.0.0
google-generativeai>=0.3
anthropic>=0.18
reportlab>=4.0.0
# OCR and image processing
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
pillow>=10.0.0
psutil>=5.9.0

# Advanced LaTeX OCR (optional but recommended)
# pix2tex[api]>=0.1.0  # Uncomment if you want transformer‑based OCR

# Commercial OCR (optional)
# aspose-ocr>=24.0.0   # Uncomment if you purchase license

# System dependencies (cannot be pip installed)
# On Linux: sudo apt-get install tesseract-ocr tesseract-ocr-eng
# On macOS: brew install tesseract
